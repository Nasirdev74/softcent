import 'package:flutter/material.dart';

class BuildRestaurant extends StatelessWidget {
  const BuildRestaurant({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
