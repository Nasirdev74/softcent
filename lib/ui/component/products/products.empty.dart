import 'package:flutter/material.dart';

class ProductsEmpty extends StatelessWidget {
  const ProductsEmpty({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
